# prints: 3.1415
print(3.1415000)

# when an integer is added to float, python silently converts int to float
# however this implicit conversion is an exception rather than a rule
# usually we have to convert manually if we want
# prints: 3.14
print(2 + 1.14)
